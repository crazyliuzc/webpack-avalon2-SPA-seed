var path = require('path');
var fs = require('fs');
var rimraf = require('rimraf');
var node_modules = path.resolve(__dirname, '../node_modules');
rimraf(node_modules, fs, function cb() {
  console.log('node_modules目录已清空');
});
